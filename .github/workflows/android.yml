name: Build Android APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Source Code
      uses: actions/checkout@v4

    - name: Install Node.js Environment
      uses: actions/setup-node@v4
      with:
        node-version: 18

    - name: Structure App Directory
      run: |
        mkdir www
        cp index.html www/

    - name: Inject Native Android Wrapper Libraries
      run: |
        npm install @capacitor/core @capacitor/cli @capacitor/android
        npx cap init "Nova_AI" "com.yourname.novaai" --web-dir=www
        npx cap add android

        # ---> FIX CODE SCRIPT: Fix the root build file before running sync <---
        sed -i 's/flatDir/google()/g' android/build.gradle
        
        npx cap sync android

    - name: Set up Java JDK Runtime
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Compile Code to Raw APK File
      run: |
        cd android
        chmod +x gradlew
        ./gradlew assembleDebug --no-daemon

    - name: Upload Finished APK for Download
      uses: actions/upload-artifact@v4
      with:
        name: nova-ai-apk
        path: android/app/build/outputs/apk/debug/app-debug.apk

